name: Publish to PyPI

permissions:
  id-token: write

on:
  release:
    types: [published]

jobs:
  release:
    name: Build and Publish
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install

      - name: Set version from GITHUB_REF
        run: |
          # GITHUB_REF will be `refs/tags/v1.2.3` for a tag named "v1.2.3"
          # we strip the `refs/tags/` part to get the version number
          # we also strip "v" from the version number, if it exists, getting "1.2.3"
          echo "GITHUB_REF: ${{ github.ref }}"
          export VERSION=$(echo ${{ github.ref }} | sed -e 's,.*/\(.*\),\1,' -e 's/^v//')
          echo Setting version to $VERSION

          # Update version in pyproject.toml
          sed -i "s/^version = \"0.0.0\"/version = \"$VERSION\"/" pyproject.toml
          echo "======================================"
          echo "Version line in pyproject.toml:"
          grep "^version" pyproject.toml
          echo "======================================"

          # Update version in __init__.py
          sed -i "s/__version__ = \"0.0.0\"/__version__ = \"$VERSION\"/" sqlalchemy_easy_softdelete/__init__.py
          echo "======================================"
          echo "Version line in __init__.py:"
          grep "__version__" sqlalchemy_easy_softdelete/__init__.py
          echo "======================================"

      - name: Build wheels and source tarball
        run: uv build

      - name: show temporary files
        run: ls -l

      - name: publish to PyPI
        uses: pypa/gh-action-pypi-publish@v1.12.4
