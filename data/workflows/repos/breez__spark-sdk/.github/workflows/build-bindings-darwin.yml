name: Build bindings for Darwin
on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git commit, tag or branch reference'
        required: true
        type: string
  workflow_call:
    inputs:
      repository:
        description: 'SDK repository, defaults to current repository'
        required: false
        type: string
      ref:
        description: 'Git commit, tag or branch reference'
        required: true
        type: string
      use-dummy-binaries:
        description: 'If true, creates dummy binaries rather than real binaries'
        required: false
        type: boolean
        default: false

jobs:
  build:
    if: ${{ !inputs.use-dummy-binaries }}
    runs-on: macOS-latest
    name: Build ${{ matrix.target }}
    strategy:
      matrix:
        target: [
          aarch64-apple-darwin,
          x86_64-apple-darwin,
        ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with: 
          ref: ${{ inputs.ref }}
          repository: ${{ inputs.repository || github.repository }}

      - name: Setup build
        uses: ./.github/actions/setup-build
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: |
          rustup target add ${{ matrix.target }}
          cargo install cargo-lipo

      - name: Build bindings
        working-directory: crates/breez-sdk/bindings
        run: cargo lipo --release --targets ${{ matrix.target }}
      
      - name: Archive bindings
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/libbreez_sdk_spark_bindings.a
      
      - name: Archive bindings
        uses: actions/upload-artifact@v4
        with:
          name: bindings-dynamic-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/libbreez_sdk_spark_bindings.dylib
  
  merge:
    runs-on: macOS-latest
    needs: build
    name: Build darwin-universal
    steps:
    - uses: actions/download-artifact@v4
      with:
        name: bindings-aarch64-apple-darwin
        path: aarch64-apple-darwin
    
    - uses: actions/download-artifact@v4
      with:
        name: bindings-dynamic-aarch64-apple-darwin
        path: dynamic-aarch64-apple-darwin

    - uses: actions/download-artifact@v4
      with:
        name: bindings-x86_64-apple-darwin
        path: x86_64-apple-darwin

    - uses: actions/download-artifact@v4
      with:
        name: bindings-dynamic-x86_64-apple-darwin
        path: dynamic-x86_64-apple-darwin

    - name: Build Darwin universal
      run: |
        mkdir -p darwin-universal
        lipo -create -output darwin-universal/libbreez_sdk_spark_bindings.a aarch64-apple-darwin/libbreez_sdk_spark_bindings.a x86_64-apple-darwin/libbreez_sdk_spark_bindings.a

    - name: Build dynamic Darwin universal
      run: |
        mkdir -p dynamic-darwin-universal
        lipo -create -output dynamic-darwin-universal/libbreez_sdk_spark_bindings.dylib dynamic-aarch64-apple-darwin/libbreez_sdk_spark_bindings.dylib dynamic-x86_64-apple-darwin/libbreez_sdk_spark_bindings.dylib

    - name: Archive release
      uses: actions/upload-artifact@v4
      with:
        name: bindings-darwin-universal
        path: darwin-universal/libbreez_sdk_spark_bindings.a

    - name: Archive dynamic release
      uses: actions/upload-artifact@v4
      with:
        name: bindings-dynamic-darwin-universal
        path: dynamic-darwin-universal/libbreez_sdk_spark_bindings.dylib

  build-dummies:
    if: ${{ inputs.use-dummy-binaries }}
    runs-on: ubuntu-latest
    name: Build dummies ${{ matrix.target }}
    strategy:
      matrix:
        target: [
          aarch64-apple-darwin,
          x86_64-apple-darwin,
          darwin-universal
        ]
    steps:
      - name: Build dummy darwin ${{ matrix.target }}
        run: touch libbreez_sdk_spark_bindings.a

      - name: Upload dummy darwin ${{ matrix.target }} artifact
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.target }}
          path: ./*

  build-dynamic-dummies:
    if: ${{ inputs.use-dummy-binaries }}
    runs-on: ubuntu-latest
    name: Build dummies ${{ matrix.target }}
    strategy:
      matrix:
        target: [
          dynamic-aarch64-apple-darwin,
          dynamic-x86_64-apple-darwin,
          dynamic-darwin-universal
        ]
    steps:
      - name: Build dummy darwin ${{ matrix.target }}
        run: touch libbreez_sdk_spark_bindings.dylib

      - name: Upload dummy darwin ${{ matrix.target }} artifact
        uses: actions/upload-artifact@v4
        with:
          name: bindings-dynamic-${{ matrix.target }}
          path: ./*