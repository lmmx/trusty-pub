name: Security Scan

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  # safety:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #
  #     - name: Install project
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -e "."
  #
  #     - name: Run Safety CLI
  #       uses: pyupio/safety-action@v1
  #       with:
  #         api-key: ${{ secrets.SAFETY_API_KEY }}

  bandit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install bandit
        run: pip install bandit[toml]

      - name: Run bandit
        run: bandit -r prompture/ -c pyproject.toml
