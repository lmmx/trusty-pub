name: Build ESP32-C3

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "ESP32-C3"
      test-dir: "tests/esp32c3"
      env-name: "esp32c3"
      firmware-ext: "bin"
