name: Build Teensy 4.1

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/template_build.yml
    with:
      workflow-name: "Teensy 4.1"
      test-dir: "tests/teensy41"
      env-name: "teensy41"
      firmware-ext: "hex"
